# Fallback per-host monitor layout. install.sh copies this to monitors.conf
# when it can't auto-detect outputs. Edit to taste after first login:
#   hyprctl monitors        # list connected outputs + modes
# Syntax: monitor = NAME, RESOLUTION@HZ, POSITION, SCALE
monitor = , preferred, auto, 1
